[project]
name = "my-ml-project"
version = "0.1.0"
description = ""
authors = [
    {name = "dmsant-3",email = "dmsant3@gmail.com"}
]
requires-python = ">=3.14"
dependencies = [
    "pandas (>=3.0.5,<4.0.0)",
    "scikit-learn (>=1.9.0,<2.0.0)",
    "xgboost (>=3.4.1,<4.0.0)",
    "mlflow (>=3.16.0,<4.0.0)",
    "wandb (>=0.30.0,<0.31.0)",
    "joblib (>=1.6.0,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.1.1,<10.0.0)",
    "ruff (>=0.16.6,<0.17.0)",
    "jupyter (>=1.1.1,<2.0.0)"
]
